---
title: Vue
description: A cva button component built with Vue.
---

import StackBlitz from "../../../components/stackblitz.astro";
import Carbon from "../../../components/carbon.astro";

<Carbon />

This example builds a `Button` component with `cva` in a Vue project. Open the sandbox below to explore the full source.

<StackBlitz dir="examples/latest/vue" file="src/components/Button.vue" />
