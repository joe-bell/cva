---
title: React with Tailwind CSS
description: A cva button component built with React and Tailwind CSS, including compound components and class conflict resolution.
---

import StackBlitz from "../../../../../components/stackblitz.astro";

These examples build a React `button` component with `cva` and Tailwind CSS. They cover compound components and conflicting Tailwind classes.

## Basic component

<StackBlitz
  dir="examples/beta/react-with-tailwindcss"
  file="src/components/button/button.tsx"
/>

## Compound components

<StackBlitz
  dir="examples/beta/react-with-tailwindcss-compound"
  file="src/components/nav/nav.tsx"
/>

## Merging Tailwind classes with tailwind-merge

This example composes [`clsx`](https://github.com/lukeed/clsx), which resolves `cva`'s authoring grammar, with [`tailwind-merge`](https://github.com/dcastil/tailwind-merge), which dedupes conflicting Tailwind utilities. This is the pattern behind shadcn/ui's [`cn` helper](https://ui.shadcn.com/docs/installation/manual#add-a-cn-helper). It's wired in as `cva`'s concatenator via [`cva/core`](/beta/api-reference#cvacore) and re-exported as `cn`. See [Handling style conflicts](/beta/getting-started/installation#handling-style-conflicts) for the details.

<StackBlitz
  dir="examples/beta/react-with-tailwind-merge"
  file="src/cva.config.ts"
/>

[`cn`](https://github.com/shadcn-ui/cn) is an alternative single-function configuration:

```ts
import { cn } from "cn";
import { defineConfig } from "cva/core";

export const { cva, cx } = defineConfig({ cx: cn });
```
